# npm 网络配置 - 基于GitHub Actions CI/CD最佳实践 2024
# 参考：Context7 MCP提供的GitHub Actions与npm配置指导

# 网络重试配置（GitHub Actions环境优化）
fetch-retries=5
fetch-retry-factor=2
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
fetch-timeout=300000

# 注册表配置
registry=https://registry.npmjs.org/

# CI/CD环境优化
# 禁用不必要的网络请求以提升安装速度
fund=false
audit=false

# 缓存配置（使用推荐的prefer-offline替代已废弃的cache-min）
# cache-min=604800  # 已废弃，删除以避免警告

# 日志级别（生产环境使用warn减少噪音）
loglevel=notice

# 网络配置
prefer-online=false
prefer-offline=true

# 安装行为配置
package-lock-only=false
save-exact=true

# 进度显示（CI环境关闭以减少日志噪音）
progress=false